---
title: "コマンド"
description: "Claude Code と Codex 向けの Nomo のコマンドをすべて — ペアリング、状態確認、承認、リセット、ペアリング解除 — 1 ページにまとめました。"
icon: "terminal"
---

コマンドは 5 つ、両方のエージェントで同じ 5 つです。対象にしたいコンピュータで、エージェントのセッションの中から実行します — Claude Code では `/` のコマンド、Codex では `$` のスキルです。

<Tip>
  Claude Code ではすべてが名前空間に入っています。`/nomo-cc:` と入力すると一覧が表示されます。
</Tip>

| | <img src="/images/claude-code.svg" alt="" className="agent-mark" /> Claude Code | <img src="/images/codex.svg" alt="" className="agent-mark" /> Codex |
| --- | --- | --- |
| <a href="#pair">このコンピュータをペアリングする</a> | `/nomo-cc:pair` | `$nomo-pair` |
| <a href="#status">状態を確認する</a> | `/nomo-cc:status` | `$nomo-status` |
| <a href="#approvals">承認を一時停止・再開する</a> | `/nomo-cc:approvals` | `$nomo-approvals` |
| <a href="#reset">停止したセッションを消去する</a> | `/nomo-cc:reset` | `$nomo-reset` |
| <a href="#unpair">このコンピュータの接続を解除する</a> | `/nomo-cc:unpair` | `$nomo-unpair` |

どれも派手に失敗することはありません。することが何もない場合でも、すべて正常に終了します — `Not paired` はエラーではなく成功です。

## pair

<CodeGroup>
```bash Claude Code icon="/images/claude-code.svg"
/nomo-cc:pair          # QR code, opens your browser
/nomo-cc:pair code     # one-time code, printed in the terminal
```

```bash Codex icon="/images/codex.svg"
$nomo-pair             # QR code, opens your browser
$nomo-pair code        # one-time code, printed in the terminal
```
</CodeGroup>

引数なしで実行すると、テーマの適用されたページがブラウザで開き、QR コードが表示されます。アプリでスキャンすると、ページは自動的に **Paired ✓** に切り替わります。

`code` 引数を付けるとブラウザを完全に省き、入力できるワンタイムコードを出力します。ヘッドレスなマシンや SSH セッション向けです。これが第 2 の選択肢なのは意図的で、そのコードはターミナルに、つまりエージェントのトランスクリプトに残るからです。

ペアリングは **10 分間**使われないと期限切れになります。1 つのペアリングが対象とするのはマシンであり、エージェントではありません — 同じ Mac の Claude Code と Codex は同じペアリングを共有します。詳しい手順は[ペアリング](/ja/setup/pairing)にあります。

## status

<CodeGroup>
```bash Claude Code icon="/images/claude-code.svg"
/nomo-cc:status
```

```bash Codex icon="/images/codex.svg"
$nomo-status
```
</CodeGroup>

何かおかしいと思ったら、まず実行するコマンドです。

```
Paired: yes (pairing 3f9a21c8…)
Worker: https://api.nomo.gg
Watchdog: running (pid 48213)
Last event sent: 8s ago
Tracked sessions: 3
Codex plugin: installed, trusted (7/7)
Codex Plan answers: bridge available (shared app-server socket accepted a connection)
```

1 行ずつ見ていきます。

<ResponseField name="Paired">
  `no` は、このマシンが一度も接続されたことがないという意味です。`pairing started, waiting for phone scan` は、ペアリングページを開いたまま一度もスキャンしていないという意味です。
</ResponseField>

<ResponseField name="Watchdog">
  実行中のセッションがあるのに `not running` でも問題ありません — 次のセッションで自動的に起動します。
</ResponseField>

<ResponseField name="Last event sent">
  セッションが実際に動いているのに `never` の場合は、フックが発火していません。Claude Code を再起動して読み込ませてください。Codex では `/hooks` を実行します。
</ResponseField>

<ResponseField name="Codex plugin">
  `installed, hooks NOT trusted (run /hooks in Codex)` が、よく人をつまずかせるものです — [Codex での利用](/ja/sessions/codex#hooks-have-to-be-trusted)をご覧ください。
</ResponseField>

<ResponseField name="Codex Plan answers">
  `bridge available` は、質問が選択肢付きで iPhone に届くという意味です。`status-only` は届かないという意味です —
  [理由](/ja/sessions/codex#questions-in-the-cli-and-the-desktop-app)。
</ResponseField>

さらに、すべてのイベントが黙って二重に報告される 2 つの構成についても警告します。ネイティブプラグイン以前の Codex のフックが残っている場合と、Codex が自身のプラグインと並んで Claude Code のプラグインを自動検出している場合です。どちらも編集すべきファイル名を示します。

## approvals

<CodeGroup>
```bash Claude Code icon="/images/claude-code.svg"
/nomo-cc:approvals off      # pause on this computer
/nomo-cc:approvals on       # resume
/nomo-cc:approvals status   # which way is it set
```

```bash Codex icon="/images/codex.svg"
$nomo-approvals off         # pause on this computer
$nomo-approvals on          # resume
$nomo-approvals status      # which way is it set
```
</CodeGroup>

**このコンピュータ**のリモート承認を一時停止し、許可リクエストが iPhone を待たずにターミナルへ直行するようにします。iPhone の電池が切れている、手元にないといったときの逃げ道です。

```
Remote approvals are OFF for this computer — Claude Code permission prompts
will appear in the terminal as usual (your phone is not asked).
```

スイッチはコンピュータごとに 1 つで、**両方のエージェントで共有されます** — Codex で一時停止すると、そのマシンの Claude Code でも一時停止されます。ペアリングを解除することも、iPhone 側の設定に触れることもありません。

iPhone 全体に効く別のスイッチもあります。[設定](/ja/sessions/settings#remote-approvals)の「iPhoneで回答」です。

## reset

<CodeGroup>
```bash Claude Code icon="/images/claude-code.svg"
/nomo-cc:reset
```

```bash Codex icon="/images/codex.svg"
$nomo-reset
```
</CodeGroup>

ウォッチドッグを停止し、無効になったセッションや実体のないセッションの行を消去して、それらを削除するよう iPhone に伝えます。

```
Stopped the watchdog (it restarts automatically on your next session).
Cleared 4 stale session record(s) (4 end signals delivered to your phone).
Left 2 live sessions untouched.
Pairing and keys were not touched — use the unpair command if you want that.
```

行が古いままだったり重複したりしているときに、まず試すべき安全なコマンドです。何に手を付けなかったかまで報告してくれます。

## unpair

<CodeGroup>
```bash Claude Code icon="/images/claude-code.svg"
/nomo-cc:unpair
```

```bash Codex icon="/images/codex.svg"
$nomo-unpair
```
</CodeGroup>

ペアリングを取り消し、そのコンピュータ上の鍵を消去します。

**iPhone から**コンピュータを削除するのは別のことです。そちらはアプリに表示されなくなるだけで、このコマンドを実行するまで Mac はローカルの状態を保持し続けます。

## 状態の保存場所

プラグインが保持するものはすべて `~/.config/cc-status/` にあります。ペアリング、暗号鍵（モードは `0600`）、追跡中のセッションごとのファイル 1 つずつ、そしてローカルのトレースログです。このフォルダの中身がどこかへ送られることはありません。

## ソースコード

これらのコマンドはプラグインそのものであり、プラグインはオープンソースです。自分のマシンで何が動き、何が外へ出るのかを正確に知りたい場合、その答えは 1 つのリポジトリにあります — マーケットプレイスからのインストールも、その単純なクローンです。

<Card title="KarrixLee/nomo" href="https://github.com/KarrixLee/nomo" icon="github" horizontal>
  プラグインの全ソースコード、MIT ライセンス。issue やバグ報告もこちらへ。
</Card>
