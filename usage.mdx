---
title: "Usage"
description: "Track how much of your plan is left, with alerts at 70%, 90%, and 100%."
icon: "gauge"
---

Usage works like [Status](/status) — a widget plus notifications — with one
extra step: it reads numbers off your account, so it needs a sign-in. Still no
Mac plugin; this one is phone-only.

<Frame caption="The Usage tab for Claude: the 5-hour window, the weekly one, and when each resets.">
  <div className="device-frame">
    <img src="/images/claude-usage-overview.png" alt="Nomo's Usage tab showing Claude's session and weekly limits" />
  </div>
</Frame>

## Signing in

Open the provider from the **Providers** tab and connect the account you
already pay for. Nomo reads the same numbers that account can see about itself
— it isn't an API key and there's nothing to paste.

<Note>
  **Your login stays on your phone.** Tokens are held in the device keychain
  and go only to the provider they belong to — your Claude token to Anthropic,
  your OpenAI token to OpenAI. Usage is fetched on-device and never passes
  through Nomo's servers.
</Note>

Nomo is unofficial and not affiliated with any provider it reads.

## Add the widget

<Card title="Adding a widget" href="/widget" icon="plus" horizontal>
  Two short clips — putting it on your Home Screen, then pointing it at a
  service.
</Card>

## Reading it

Two windows, tracked separately:

| | |
| --- | --- |
| **Session** | A rolling 5-hour window. |
| **Weekly** | A rolling 7-day window. |

Each shows a percentage and when it resets. You can switch the number between
**Used** and **Left** in Settings, whichever way round you prefer to think
about it.

{/* TODO: what counts against a window — messages, tokens, or both? */}

## Threshold notifications

You get a notification at **70%**, **90%**, and **100%** — for the 5-hour
window and the weekly one separately, so a heavy afternoon and a heavy week
each warn you in their own right.

They're off until you switch them on, per account.

<Card title="Notifications" href="/notifications" icon="bell" horizontal>
  Turning alerts on, and picking which accounts they cover.
</Card>

## When it refreshes

{/* TODO: refresh cadence, and why the number can lag */}
